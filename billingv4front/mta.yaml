_schema-version: "3.2"
ID: billingv4front
description: Generated by Fiori Tools
version: 0.0.1
modules:
  - name: billingv4front-destination-content
    type: com.sap.application.content
    requires:
      - name: billingv4front-destination-service
        parameters:
          content-target: true
      - name: billingv4front-repo-host
        parameters:
          service-key:
            name: billingv4front-repo-host-key
      - name: billingv4front-uaa
        parameters:
          service-key:
            name: billingv4front-uaa-key
    parameters:
      content:
        instance:
          destinations:
            - Name: billingv4front_html_repo_host
              ServiceInstanceName: billingv4front-html5-service
              ServiceKeyName: billingv4front-repo-host-key
              sap.cloud.service: billingv4front
            - Authentication: OAuth2UserTokenExchange
              Name: billingv4front_uaa
              ServiceInstanceName: billingv4front-xsuaa-service
              ServiceKeyName: billingv4front-uaa-key
              sap.cloud.service: billingv4front
          existing_destinations_policy: update
    build-parameters:
      no-source: true
  - name: billingv4front-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: billingv4front-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - billingv4front.zip
          name: billingv4front
          target-path: resources/
  - name: billingv4front
    type: html5
    path: .
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: billingv4front-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: billingv4front-destination-service
      service-plan: lite
  - name: billingv4front-uaa
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: billingv4front-xsuaa-service
      service-plan: application
  - name: billingv4front-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: billingv4front-html5-service
      service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
